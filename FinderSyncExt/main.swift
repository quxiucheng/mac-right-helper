import Foundation

/// Finder Sync Extension executable stub.
/// Finder loads the extension class directly via NSExtensionPrincipalClass.
/// This @main stub satisfies the linker requirement for a `_main` symbol.
@main
struct FinderSyncExtMain {
    static func main() {
        // No-op — the extension lifecycle is fully managed by the NSExtension host (Finder).
    }
}
